import { EtatDirective } from './etat.directive';

describe('EtatDirective', () => {
  it('should create an instance', () => {
    const directive = new EtatDirective();
    expect(directive).toBeTruthy();
  });
});
